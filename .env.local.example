# OpenAI API Key (required)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: OpenAI model selection for video analysis (default: gpt-4o)
# Available models:
# - gpt-4o (default: most capable, best quality)
# - gpt-4o-mini (faster, cheaper alternative)
# - gpt-4-turbo (previous gen, still very good)
# - gpt-3.5-turbo (cheapest, basic quality)
OPENAI_MODEL=gpt-4o

# Optional: OpenAI model selection for blog generation (default: gpt-4o)
# Blog generation requires higher quality writing, so gpt-4o or gpt-4-turbo recommended
# - gpt-4o (recommended: best quality, $15/1M tokens)
# - gpt-4-turbo (good quality, $10/1M tokens)
# - gpt-4o-mini (budget option, $0.15/1M tokens, lower quality)
OPENAI_BLOG_MODEL=gpt-4o

# Optional: Screenshot API for blog post images
# Without this, blog posts will use default YouTube thumbnails
# With this, actual video frames at specific timestamps will be captured
# Recommended service: screenshotone.com (https://screenshotone.com/)
# Pricing: ~$29/month for 3000 screenshots, or pay-as-you-go
# SCREENSHOT_API_KEY=your-screenshotone-access-key

# Optional: Cache settings
# CACHE_ENABLED=true
# CACHE_DIR=.cache/analyses
